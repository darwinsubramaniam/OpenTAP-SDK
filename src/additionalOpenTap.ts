/**
 * You can also specify a package that you just want installed (in e.g. bin/Debug/)
 * but don't want your project to reference.
 * This can be useful for defining a larger context in which to debug.
 */
 export interface AdditionalOpenTapPlugin{
    name:string,
    version:string,
    repo:string,
}

